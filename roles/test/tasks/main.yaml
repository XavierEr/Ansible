---
- name: Download test source
  git:
    repo: https://github.com/XavierEr/test.git
    dest: github/test/
    update: no

- name: Install packages based on package.json.
  npm:
    path: /github/test
    state: present

- name: Run test server
  shell: node server.js >> log.txt
  args:
    chdir: /github/test
    creates: log.txt
...